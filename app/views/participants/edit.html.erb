<% content_for :title, "Editing participant" %>

<h1>Editing participant</h1>

<%= render "form", participant: @participant %>

<br>

<div>
  <%= link_to "Show this participant", project_participant_path(@project, @participant) %>
  <%= link_to "Back to participants", project_participants_path(@project) %>
</div>
