<%= form_with(model: [@project, @participant]) do |form| %>
  <% if participant.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(participant.errors.count, "error") %> prohibited this participant from being saved:</h2>

      <ul>
        <% participant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :first_name, style: "display: block" %>
    <%= form.text_field :first_name, class: "border border-gray-300 rounded-md p-2 w-full" %>
  </div>

  <div>
    <%= form.label :last_name, style: "display: block" %>
    <%= form.text_field :last_name, class: "border border-gray-300 rounded-md p-2 w-full" %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.number_field :status, class: "border border-gray-300 rounded-md p-2 w-full" %>
  </div>

  <div>
    <%= form.label :spouse_id, style: "display: block" %>
    <%= form.number_field :spouse_id, class: "border border-gray-300 rounded-md p-2 w-full" %>
  </div>

  <div>
    <%= form.label :parent_id, style: "display: block" %>
    <%= form.number_field :parent_id, class: "border border-gray-300 rounded-md p-2 w-full" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
