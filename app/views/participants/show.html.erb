<p style="color: green"><%= notice %></p>

<%= render @participant %>

<div>
  <%= link_to "Edit this participant", edit_project_participant_path(@project, @participant) %>
  <%= link_to "Back to participants", project_participants_path(@project) %>

  <%= button_to "Destroy this participant", project_participant_path(@project, @participant), method: :delete %>
</div>
